apiVersion: v1
kind: Service
metadata:
  name: postgres18
  labels:
    app: postgres
spec:
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432 # Service port
      targetPort: 5432 # Container port
